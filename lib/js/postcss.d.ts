/**
 * Watches the CSS directory and rebuilds CSS on changes.
 *
 * - Rebuilds CSS when the main file changes
 * - Deletes output files when the source is removed
 * - Logs all watcher events
 */
export declare const watchCss: () => void;
