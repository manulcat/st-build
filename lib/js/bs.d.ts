/**
 * Starts the BrowserSync server with the generated options.
 *
 * @param {Function} onReady - Callback executed when the server is ready.
 */
export declare const watchServer: (onReady?: () => void) => void;
